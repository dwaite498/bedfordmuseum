version: '3'
services:
  db:
    image: mysql:8
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
    logging:
      options:
        max-size: 5m
        max-file: '10'
    volumes:
      - ${DATA_DIR}/db:/var/lib/mysql
  rails:
    image: bedford_rails
    # volumes:
    #   - ${DATA_DIR}/rails/public/system:/app/public/system
    #   - ${CONFIG_DIR}/rails_secrets.yml:/app/config/secrets.yml:ro
    depends_on:
      - db
    logging:
      options:
        max-size: 5m
        max-file: '10'
    restart: always
    environment:
      - ENVIRONMENT
  nginx:
    image: nginx:alpine
    ports:
      - ${PORT}:${PORT}
    volumes:
      - ${CONFIG_DIR}/nginx/default.template:/etc/nginx/conf.d/default.template:ro
      - ${CONFIG_DIR}/nginx/htpasswd:/etc/apache2/.htpasswd:ro
      - ${DATA_DIR}/sftp/bedfordvamuseum:/usr/share/nginx/html:ro
    depends_on:
      - rails
    logging:
      options:
        max-size: 5m
        max-file: '10'
    restart: always
    environment:
      - PORT
      - SERVER_NAME
    command: "/bin/sh -c \"apk add --no-cache --virtual templating gettext \
      && envsubst '$$SERVER_NAME $$PORT' < /etc/nginx/conf.d/default.template > /etc/nginx/conf.d/default.conf \
      && apk del templating \
      && exec nginx -g 'daemon off;'\""
